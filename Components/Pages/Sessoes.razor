@page "/sessoes"

<PageTitle>Sessões - NutriAI</PageTitle>

<div class="d-flex justify-space-between align-center mb-6">
    <div>
        <MudText Typo="Typo.h5" Class="font-bold">Sessões</MudText>
        <MudText Typo="Typo.body2" Style="color: #6b7280;">Registre e acompanhe as consultas</MudText>
    </div>
    <MudButton Variant="Variant.Filled" Color="Color.Primary" StartIcon="@Icons.Material.Filled.Add" Class="rounded-lg text-none px-4">
        Nova Sessão
    </MudButton>
</div>

<!-- Sessions List -->
<div class="d-flex flex-column gap-4">
    @foreach (var s in sessoes)
    {
        <MudPaper Class="nutri-card pa-4 d-flex justify-space-between align-start" Elevation="0">
            <div class="d-flex gap-4">
                <div class="stat-icon-wrapper icon-blue" style="width: 48px; height: 48px; flex-shrink: 0;">
                    <MudIcon Icon="@Icons.Material.Filled.EventNote" Size="Size.Medium" />
                </div>
                <div>
                    <div class="d-flex align-center gap-3 mb-1">
                        <MudText Typo="Typo.subtitle1" Class="font-bold">@s.PacienteNome</MudText>
                        <span class="status-badge" style="background-color: #eaf1fa; color: #5086e9;">@s.Tipo</span>
                    </div>
                    
                    <MudText Typo="Typo.caption" Style="color: #a0aabf; display: block;" Class="mb-3">
                        @s.DataSessao.ToString("yyyy-MM-dd")
                    </MudText>

                    <div class="d-flex align-center gap-2 mb-2">
                        <MudText Typo="Typo.body2" Style="color: #6b7280;">Peso: @s.PesoSessao kg</MudText>
                    </div>

                    <MudText Typo="Typo.body2" Style="color: #6b7280;" Class="mb-2">@s.Anotacoes</MudText>

                    <MudText Typo="Typo.caption" Style="color: #2bd07b; font-weight: 500;">
                        Próxima consulta: @(s.ProximaConsulta?.ToString("yyyy-MM-dd") ?? "A definir")
                    </MudText>
                </div>
            </div>
            
            <MudIconButton Icon="@Icons.Material.Filled.DeleteOutline" Size="Size.Small" Color="Color.Error" />
        </MudPaper>
    }
</div>

@code {
    private List<SessaoViewModel> sessoes = new()
    {
        new() { 
            PacienteNome = "Ana Paula Ferreira", 
            DataSessao = new DateTime(2025, 03, 15), 
            Tipo = "Retorno", 
            PesoSessao = 71.2m, 
            Anotacoes = "Retorno positivo. Perdeu 1.3kg. Ajuste no lanche da tarde.", 
            ProximaConsulta = new DateTime(2025, 04, 01) 
        },
        new() { 
            PacienteNome = "Ana Paula Ferreira", 
            DataSessao = new DateTime(2025, 03, 01), 
            Tipo = "Consulta Inicial", 
            PesoSessao = 72.5m, 
            Anotacoes = "Primeira consulta. Paciente motivada. Iniciou plano hipocalórico com controle glicêmico.", 
            ProximaConsulta = new DateTime(2025, 03, 15) 
        },
        new() { 
            PacienteNome = "Carlos Eduardo Silva", 
            DataSessao = new DateTime(2025, 03, 01), 
            Tipo = "Consulta Inicial", 
            PesoSessao = 88.0m, 
            Anotacoes = "Avaliação inicial. Objetivo de hipertrofia. Plano hiperproteico elaborado.", 
            ProximaConsulta = new DateTime(2025, 04, 01) 
        }
    };

    public class SessaoViewModel
    {
        public string PacienteNome { get; set; } = "";
        public DateTime DataSessao { get; set; }
        public string Tipo { get; set; } = "";
        public decimal PesoSessao { get; set; }
        public string Anotacoes { get; set; } = "";
        public DateTime? ProximaConsulta { get; set; }
    }
}
